language: python
python:
  - "3.12"  # Updated to match pyproject.toml

notifications:
  email: false

before_install:
  - pip install poetry  # Install Poetry

install:
  - poetry install  # Install dependencies and build extension

script:
  - poetry run pytest cython_example_proj  # Run tests within Poetry environment
